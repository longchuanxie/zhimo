# 知识库按需调研 - 阶段 5：版本管理激活 实施计划

## 一、背景与整体进度

本项目为 AI 原生写作项目（Tauri + React + TypeScript + SQLite），针对小说编写场景增强项目级知识库的「按需调研」能力。整体方案分 5 个阶段：

| 阶段 | 内容 | 状态 |
|------|------|------|
| 阶段 1 | 模型网关 Tool Use 支持 | 已完成 |
| 阶段 2 | 知识查询工具 + Agent 工具调用循环 | 已完成 |
| 阶段 3 | 知识类型结构化（character/setting/worldview/plot/rule/fact） | 已完成 |
| 阶段 4 | 三种知识自动提取（文档/对话/资料） | 已完成 |
| 阶段 5 | 版本管理激活（version + replacedById） | **本计划目标** |

本计划仅覆盖阶段 5：激活知识表的 `version` 与 `replacedById` 字段，提供「创建新版本」能力与版本链路可视化，支撑小说创作中设定演进（如人物成长、世界观调整）的可追溯性。

---

## 二、当前状态分析

### 2.1 数据访问层（knowledgeRepository.ts）

**已有基础：**
- `markReplacedBy(oldId, newId)` 函数已存在（L198-206），会将旧知识 `status` 置为 `'deprecated'` 并写入 `replaced_by_id`
- `Knowledge` 类型字段映射中已包含 `version` 和 `replacedById`（L24-25）
- 行映射函数 `mapKnowledge` 已正确处理 `version: Number(...)`（L40）

**缺失：**
- `insertKnowledge` 当前 SQL **硬编码 `version=1`**（L107），不支持自定义版本号
- 没有"按指定 version 插入"的方法
- 没有"按 replacedById 反查旧版本"的方法（用于详情页向上回溯）

### 2.2 业务编排层（KnowledgeService.ts，314 行）

**当前导出方法：** `listKnowledge` / `getKnowledge` / `createKnowledge` / `updateKnowledge` / `updateKnowledgeStatusService` / `updateKnowledgeAiUsageService` / `updateKnowledgeConfidenceService` / `deleteKnowledge`

**缺失：**
- 没有 `replaceKnowledge` 方法
- `markReplacedBy` 未被 import，版本演进逻辑未接入 Service 层
- `createKnowledge` 调用 `insertKnowledge` 时无法传入 version

### 2.3 UI 层（KnowledgeDetailPage.tsx，488 行）

**当前状态：**
- 元信息行仅静态显示 `版本 v{item.version}`（L368），无任何交互
- 版本演进区块（L475-483）极简陋：仅当 `item.replacedById` 存在时显示一行文字 `此知识已被新版本替换：{item.replacedById}`，**没有跳转链接、没有完整链路、没有"被谁替换前的旧版本"信息**
- 没有「创建新版本」按钮

**可维护性风险：**
- 文件已达 488 行，远超 AGENTS.md §3.3 单组件 250 行阈值
- 新增版本演进 UI 必须拆出独立子组件，避免进一步膨胀

---

## 三、假设与决策

### 3.1 假设
1. `knowledge` 表 schema 已包含 `version INTEGER` 与 `replaced_by_id TEXT` 字段（基于 knowledgeRepository 字段映射推断，无需迁移）
2. 项目使用现有的 `ok`/`err`/`fromUnknown` ServiceResult 模式
3. 项目使用现有的 `useDialog`/`toast` 反馈机制
4. 现有错误码 `NOT_FOUND`/`VALIDATION_ERROR` 可复用

### 3.2 关键决策

**决策 1：版本号生成策略 — 基于 old.version + 1**
- 不查询整个版本链取 max(version)，因为版本链可能因软删除而断裂
- 直接 `old.version + 1` 简单可靠，符合"线性演进"语义

**决策 2：在已 deprecated 的版本上是否允许创建新版本 — 不允许**
- 已被替换的旧版本代表"历史快照"，不应再衍生新分支
- UI 在 `item.status === 'deprecated'` 时禁用「创建新版本」按钮
- Service 层增加校验，deprecated 版本调用 replaceKnowledge 返回 VALIDATION_ERROR

**决策 3：replaceKnowledge 后的页面行为 — 自动跳转到新版本详情页**
- 旧版本已被标记 deprecated，继续停留在旧页面会误导用户
- 跳转后用户可立即查看新版本内容并继续编辑

**决策 4：版本链路展示范围 — 仅展示相邻关系 + 链路入口**
- 详情页主体展示"当前版本的上一个版本"和"当前版本替换到的下一个版本"
- 不在详情页主体展示完整版本链（避免性能问题与 UI 膨胀）
- 如需查看完整链路，通过"上一个版本"链接逐级回溯（符合线性演进语义）

**决策 5：UI 组件拆分 — 拆出 KnowledgeVersionChain 子组件**
- KnowledgeDetailPage.tsx 已 488 行，新增版本演进 UI 必须拆分
- 拆出 `KnowledgeVersionChain` 子组件负责版本链路展示
- 拆出 `ReplaceKnowledgeModal` 子组件负责创建新版本表单

**决策 6：事务边界 — Service 层组合两个 Repository 调用**
- SQLite 本地无真正的事务 API（基于现有 execute 模式），采用"先插入新版本，再 markReplacedBy"顺序
- 若 markReplacedBy 失败，新版本已插入但旧版本未标记，属于可接受中间态（用户可手动 deprecated 旧版本）
- 不引入事务封装，避免改动 db 层架构

---

## 四、改造方案

### 4.1 改造 `src/services/database/knowledgeRepository.ts`

**目标：** 新增版本化插入方法 + 反查方法

**改动 1：新增 `insertKnowledgeVersion` 方法**

在现有 `insertKnowledge` 函数（L89-123）之后新增：

```ts
/// 创建知识（指定版本号，用于版本演进）
/// 与 insertKnowledge 的区别：version 由调用方指定，而非硬编码为 1
export async function insertKnowledgeVersion(input: {
  id: EntityId
  projectId: EntityId
  title: string
  type: string
  content: string
  summary: string | null
  sourceType: string | null
  sourceId: EntityId | null
  aiUsageAllowed: boolean
  confidence: number | null
  version: number
}): Promise<void> {
  const timestamp = now()
  await execute(
    `INSERT INTO knowledge (
      id, project_id, title, type, content, summary, status,
      source_type, source_id, ai_usage_allowed, confidence,
      version, replaced_by_id, is_deleted, deleted_at, created_at, updated_at
    ) VALUES (?, ?, ?, ?, ?, ?, 'pending', ?, ?, ?, ?, ?, NULL, 0, NULL, ?, ?)`,
    [
      input.id,
      input.projectId,
      input.title,
      input.type,
      input.content,
      input.summary,
      input.sourceType,
      input.sourceId,
      input.aiUsageAllowed ? 1 : 0,
      input.confidence,
      input.version,
      timestamp,
      timestamp,
    ],
  )
}
```

**改动 2：新增 `findKnowledgeByReplacedById` 反查方法**

在 `markReplacedBy` 函数（L198-206）之后新增：

```ts
/// 反查：被指定新版本替换的旧版本（即 replaced_by_id = newId 的记录）
/// 用于版本链路上溯
export async function findKnowledgeByReplacedById(
  newId: EntityId,
): Promise<Knowledge | null> {
  const rows = await select<Record<string, unknown>>(
    'SELECT * FROM knowledge WHERE replaced_by_id = ? AND is_deleted = 0',
    [newId],
  )
  if (rows.length === 0) return null
  return mapKnowledge(rows[0]!)
}
```

**理由：**
- `insertKnowledgeVersion` 与 `insertKnowledge` 仅 version 字段不同，独立函数比可选参数更清晰（避免破坏 insertKnowledge 现有调用方）
- `findKnowledgeByReplacedById` 用于详情页"此版本替换了哪个旧版本"展示

---

### 4.2 改造 `src/services/knowledge/KnowledgeService.ts`

**目标：** 新增 `replaceKnowledge` Service 方法

**改动 1：扩展 import**

在现有 import 块中补充：

```ts
import {
  listKnowledge as repoListKnowledge,
  findKnowledgeById,
  insertKnowledge,
  insertKnowledgeVersion,  // 新增
  updateKnowledgeContent,
  updateKnowledgeStatus,
  updateKnowledgeAiUsage,
  updateKnowledgeConfidence,
  markReplacedBy,            // 新增
  softDeleteKnowledge,
} from '@/services/database/knowledgeRepository'
```

**改动 2：新增 `ReplaceKnowledgeInput` 类型**

在文件类型定义区新增：

```ts
export type ReplaceKnowledgeInput = {
  /// 旧知识 ID（被替换的版本）
  oldKnowledgeId: EntityId
  /// 新版本的可编辑字段
  title: string
  type: string
  content: string
  summary?: string
  confidence?: number | null
}
```

**改动 3：新增 `replaceKnowledge` 方法**

在 `createKnowledge` 方法之后新增：

```ts
/// 创建知识的新版本（版本演进）
///
/// 业务流程：
/// 1. 查询旧知识，校验存在且状态非 deprecated
/// 2. 计算 newVersion = old.version + 1
/// 3. 调用 insertKnowledgeVersion 写入新知识（status='pending'，继承 projectId/sourceType/sourceId/aiUsageAllowed）
/// 4. 调用 markReplacedBy(oldId, newId) 将旧知识标记为 deprecated
/// 5. 返回新知识对象
///
/// @param input.oldKnowledgeId  旧知识 ID
/// @param input.title/type/content/summary/confidence  新版本的可编辑字段
export async function replaceKnowledge(
  input: ReplaceKnowledgeInput,
): Promise<ServiceResult<Knowledge>> {
  try {
    // 校验输入
    if (!input.title.trim()) {
      return err({
        code: VALIDATION_ERROR,
        message: '知识标题不能为空',
        retryable: false,
      })
    }
    if (!input.content.trim()) {
      return err({
        code: VALIDATION_ERROR,
        message: '知识内容不能为空',
        retryable: false,
      })
    }

    // 1. 查询旧知识
    const old = await findKnowledgeById(input.oldKnowledgeId)
    if (!old) {
      return err({
        code: NOT_FOUND,
        message: '没有找到要替换的旧知识',
        retryable: false,
      })
    }

    // 2. 校验旧知识状态（已被替换的版本不能再衍生新版本）
    if (old.status === 'deprecated') {
      return err({
        code: VALIDATION_ERROR,
        message: '已被替换的旧版本不能再创建新版本',
        retryable: false,
      })
    }

    // 3. 创建新版本（继承 projectId/sourceType/sourceId/aiUsageAllowed）
    const newId = generateId()
    const newVersion = old.version + 1
    await insertKnowledgeVersion({
      id: newId,
      projectId: old.projectId,
      title: input.title.trim(),
      type: input.type,
      content: input.content,
      summary: input.summary?.trim() || null,
      sourceType: old.sourceType,
      sourceId: old.sourceId,
      aiUsageAllowed: old.aiUsageAllowed,
      confidence: input.confidence ?? null,
      version: newVersion,
    })

    // 4. 标记旧知识为 deprecated
    await markReplacedBy(old.id, newId)

    // 5. 查询并返回新知识
    const created = await findKnowledgeById(newId)
    if (!created) {
      return err({
        code: 'UNKNOWN_ERROR',
        message: '新版本创建后查询失败',
        retryable: true,
      })
    }
    return ok(created)
  } catch (error) {
    return err(fromUnknown(error))
  }
}
```

**改动 4：新增 `getPreviousVersion` 辅助方法（供 UI 查询版本链路上溯）**

```ts
/// 查询当前版本的上一个旧版本（即被当前版本替换的旧知识）
///
/// 用于详情页版本链路展示
///
/// @param currentId 当前知识 ID
/// @returns 旧版本知识（若无则 null）
export async function getPreviousVersion(
  currentId: EntityId,
): Promise<ServiceResult<Knowledge | null>> {
  try {
    const previous = await findKnowledgeByReplacedById(currentId)
    return ok(previous)
  } catch (error) {
    return err(fromUnknown(error))
  }
}
```

**理由：**
- Service 层负责编排"插入新版本 + 标记旧版本"的完整业务流程
- 校验逻辑集中在 Service 层，符合 AGENTS.md §3.4
- `getPreviousVersion` 封装 Repository 反查，避免 UI 直接访问 Repository

---

### 4.3 新建 `src/components/knowledge/KnowledgeVersionChain.tsx`

**目标：** 独立子组件，负责版本链路展示，避免 KnowledgeDetailPage 进一步膨胀

**实现：**

```tsx
import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import {
  ArrowUturnUpIcon,
  ArrowDownIcon,
} from '@heroicons/react/24/outline'
import { AppIcon } from '@/components/foundation/AppIcon'
import { getPreviousVersion } from '@/services/knowledge/KnowledgeService'
import type { Knowledge } from '@/types'

interface KnowledgeVersionChainProps {
  /// 当前知识
  current: Knowledge
  /// 项目 ID（用于构建跳转链接）
  projectId: string
}

/// 知识版本链路展示
///
/// 展示三种关系：
/// 1. 上一个版本（被当前版本替换的旧版本）— 通过 getPreviousVersion 反查
/// 2. 下一个版本（当前版本被谁替换）— 通过 current.replacedById 直接展示
/// 3. 当前版本标识（v{version}）
///
/// 若没有任何版本关系（既无上一版也无下一版），返回 null，区块不渲染
export function KnowledgeVersionChain({ current, projectId }: KnowledgeVersionChainProps) {
  const [previous, setPrevious] = useState<Knowledge | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    let cancelled = false
    async function load() {
      setLoading(true)
      const result = await getPreviousVersion(current.id)
      if (!cancelled) {
        setPrevious(result.ok ? result.data : null)
        setLoading(false)
      }
    }
    load()
    return () => { cancelled = true }
  }, [current.id])

  // 既无上一版也无下一版，且非 v1 → 不渲染
  const hasPrevious = previous !== null
  const hasNext = !!current.replacedById
  if (!hasPrevious && !hasNext && current.version === 1) return null

  return (
    <div className="card p-4 space-y-3">
      <div className="flex items-center gap-2 text-sm font-semibold text-ink">
        <AppIcon icon={ArrowUturnUpIcon} size="sm" />
        版本演进
      </div>

      {/* 上一个版本 */}
      {loading ? (
        <div className="text-xs text-muted">加载版本链路...</div>
      ) : hasPrevious ? (
        <div className="flex items-center gap-2 text-sm">
          <span className="text-subtle">上一个版本：</span>
          <Link
            to={`/projects/${projectId}/knowledge/${previous!.id}`}
            className="text-brand hover:underline"
          >
            v{previous!.version} · {previous!.title}
          </Link>
          <span className="text-xs text-muted">（已被当前版本替换）</span>
        </div>
      ) : null}

      {/* 当前版本 */}
      <div className="flex items-center gap-2 text-sm">
        <span className="text-subtle">当前版本：</span>
        <span className="px-2 py-0.5 rounded bg-brand-soft text-brand text-xs font-semibold">
          v{current.version}
        </span>
      </div>

      {/* 下一个版本 */}
      {hasNext ? (
        <div className="flex items-center gap-2 text-sm">
          <AppIcon icon={ArrowDownIcon} size="sm" className="text-muted" />
          <span className="text-subtle">已被新版本替换：</span>
          <Link
            to={`/projects/${projectId}/knowledge/${current.replacedById}`}
            className="text-brand hover:underline"
          >
            查看新版本
          </Link>
        </div>
      ) : null}
    </div>
  )
}
```

---

### 4.4 新建 `src/components/knowledge/ReplaceKnowledgeModal.tsx`

**目标：** 创建新版本的弹窗表单，复用 CreateKnowledgeModal 的结构

**实现：**

```tsx
import { useState, useEffect } from 'react'
import { Modal } from '@/components/foundation/Modal'
import { AppIcon } from '@/components/foundation/AppIcon'
import { replaceKnowledge } from '@/services/knowledge/KnowledgeService'
import { toast } from '@/stores/toastStore'
import {
  KNOWLEDGE_TYPES,
  KNOWLEDGE_TYPE_LABEL,
  type KnowledgeType,
} from '@/constants/knowledgeTypes'
import {
  ArrowUturnUpIcon,
} from '@heroicons/react/24/outline'
import type { Knowledge } from '@/types'

interface ReplaceKnowledgeModalProps {
  /// 旧知识（被替换的版本）
  oldKnowledge: Knowledge
  /// 项目 ID
  projectId: string
  /// 是否打开
  open: boolean
  /// 关闭回调
  onClose: () => void
  /// 创建成功回调（参数为新知识 ID，用于跳转）
  onReplaced: (newId: string) => void
}

/// 创建新版本弹窗
///
/// 预填旧知识字段，用户编辑后调用 replaceKnowledge
/// 成功后触发 onReplaced(newId)，由父组件跳转到新版本详情页
export function ReplaceKnowledgeModal({
  oldKnowledge,
  projectId: _projectId,
  open,
  onClose,
  onReplaced,
}: ReplaceKnowledgeModalProps) {
  const [title, setTitle] = useState('')
  const [type, setType] = useState<KnowledgeType>('character')
  const [content, setContent] = useState('')
  const [summary, setSummary] = useState('')
  const [confidence, setConfidence] = useState<number | ''>('')
  const [saving, setSaving] = useState(false)

  // 打开时预填旧知识字段
  useEffect(() => {
    if (open) {
      setTitle(oldKnowledge.title)
      setType(oldKnowledge.type as KnowledgeType)
      setContent(oldKnowledge.content)
      setSummary(oldKnowledge.summary ?? '')
      setConfidence(oldKnowledge.confidence ?? '')
    }
  }, [open, oldKnowledge])

  const handleSave = async () => {
    if (!title.trim() || !content.trim()) {
      toast.error('标题和内容不能为空')
      return
    }

    setSaving(true)
    const result = await replaceKnowledge({
      oldKnowledgeId: oldKnowledge.id,
      title: title.trim(),
      type,
      content,
      summary: summary.trim() || undefined,
      confidence: confidence === '' ? null : Number(confidence),
    })
    setSaving(false)

    if (result.ok) {
      toast.success(`已创建新版本 v${result.data.version}，旧版本已标记为废弃`)
      onReplaced(result.data.id)
    } else {
      toast.error(`创建新版本失败：${result.error.message}`)
    }
  }

  return (
    <Modal
      open={open}
      onClose={onClose}
      title={`创建新版本（当前 v${oldKnowledge.version} → 新建 v${oldKnowledge.version + 1}）`}
      size="lg"
      footer={
        <>
          <button type="button" className="btn-ghost" onClick={onClose} disabled={saving}>
            取消
          </button>
          <button
            type="button"
            className="btn-primary"
            onClick={handleSave}
            disabled={saving || !title.trim() || !content.trim()}
          >
            <AppIcon icon={ArrowUturnUpIcon} size="sm" />
            {saving ? '创建中...' : '创建新版本'}
          </button>
        </>
      }
    >
      <div className="space-y-4">
        <div>
          <label className="block text-sm font-semibold text-ink mb-1">标题</label>
          <input
            type="text"
            className="input w-full"
            value={title}
            onChange={(e) => setTitle(e.target.value)}
            disabled={saving}
            maxLength={200}
          />
        </div>

        <div>
          <label className="block text-sm font-semibold text-ink mb-1">类型</label>
          <select
            className="input w-full"
            value={type}
            onChange={(e) => setType(e.target.value as KnowledgeType)}
            disabled={saving}
          >
            {KNOWLEDGE_TYPES.map((t) => (
              <option key={t} value={t}>
                {KNOWLEDGE_TYPE_LABEL[t]}
              </option>
            ))}
          </select>
        </div>

        <div>
          <label className="block text-sm font-semibold text-ink mb-1">内容</label>
          <textarea
            className="input w-full min-h-[200px] font-mono text-sm"
            value={content}
            onChange={(e) => setContent(e.target.value)}
            disabled={saving}
          />
        </div>

        <div>
          <label className="block text-sm font-semibold text-ink mb-1">摘要（可选）</label>
          <input
            type="text"
            className="input w-full"
            value={summary}
            onChange={(e) => setSummary(e.target.value)}
            disabled={saving}
            maxLength={500}
          />
        </div>

        <div>
          <label className="block text-sm font-semibold text-ink mb-1">置信度（0-1，可选）</label>
          <input
            type="number"
            className="input w-full"
            value={confidence}
            onChange={(e) => setConfidence(e.target.value === '' ? '' : Number(e.target.value))}
            disabled={saving}
            min={0}
            max={1}
            step={0.1}
          />
        </div>

        <p className="text-xs text-muted">
          创建新版本后，旧版本（v{oldKnowledge.version}）将自动标记为「已废弃」，但不会被删除，可在版本链路中查看。
        </p>
      </div>
    </Modal>
  )
}
```

---

### 4.5 改造 `src/features/knowledge/KnowledgeDetailPage.tsx`

**目标：** 接入新版本按钮 + 替换简陋的版本演进区块

**改动 1：扩展 import**

```ts
import {
  ArrowLeftIcon,
  ArrowUturnUpIcon,  // 新增
} from '@heroicons/react/24/outline'
// ... 其他现有 import
import { replaceKnowledge } from '@/services/knowledge/KnowledgeService'  // 实际不直接调用，仅用于类型
import { KnowledgeVersionChain } from '@/components/knowledge/KnowledgeVersionChain'  // 新增
import { ReplaceKnowledgeModal } from '@/components/knowledge/ReplaceKnowledgeModal'  // 新增
import { useNavigate } from 'react-router-dom'  // 新增（用于替换成功后跳转）
```

**改动 2：新增 state**

```ts
const navigate = useNavigate()
const [showReplaceModal, setShowReplaceModal] = useState(false)
```

**改动 3：在顶部导航栏增加「创建新版本」按钮**

在现有「编辑/保存」按钮组附近（状态流转按钮区域），增加：

```tsx
{!isEditing && item.status !== 'deprecated' && (
  <button
    type="button"
    className="btn-secondary"
    onClick={() => setShowReplaceModal(true)}
    title="基于当前版本创建新版本，旧版本将自动标记为废弃"
  >
    <AppIcon icon={ArrowUturnUpIcon} size="sm" />
    创建新版本
  </button>
)}
```

**改动 4：替换简陋的版本演进区块（L475-483）**

删除原有：

```tsx
{item.replacedById && (
  <div className="...">
    此知识已被新版本替换：{item.replacedById}
  </div>
)}
```

替换为：

```tsx
<KnowledgeVersionChain current={item} projectId={projectId!} />
```

**改动 5：在文件末尾渲染 ReplaceKnowledgeModal**

```tsx
<ReplaceKnowledgeModal
  oldKnowledge={item}
  projectId={projectId!}
  open={showReplaceModal}
  onClose={() => setShowReplaceModal(false)}
  onReplaced={(newId) => {
    setShowReplaceModal(false)
    navigate(`/projects/${projectId}/knowledge/${newId}`)
  }}
/>
```

**改动 6：移除冗余的 replacedById 文字显示**

L368 区域原"版本 v{item.version}"静态显示可保留（作为元信息），版本链路由 KnowledgeVersionChain 接管。

---

## 五、文件清单

### 5.1 新建文件（2 个）
1. `src/components/knowledge/KnowledgeVersionChain.tsx` — 版本链路展示子组件
2. `src/components/knowledge/ReplaceKnowledgeModal.tsx` — 创建新版本弹窗

### 5.2 改造文件（3 个）
1. `src/services/database/knowledgeRepository.ts` — 新增 `insertKnowledgeVersion` + `findKnowledgeByReplacedById`
2. `src/services/knowledge/KnowledgeService.ts` — 新增 `replaceKnowledge` + `getPreviousVersion` + `ReplaceKnowledgeInput` 类型
3. `src/features/knowledge/KnowledgeDetailPage.tsx` — 接入新版本按钮 + 替换版本演进区块 + 渲染 Modal

---

## 六、验证清单

### 6.1 类型检查
```bash
pnpm typecheck
```
预期：零类型错误

### 6.2 构建检查
```bash
pnpm build
```
预期：构建成功

### 6.3 功能验证（手动）
1. **正常路径**：
   - 打开一个 v1 知识详情页 → 点击「创建新版本」→ 编辑内容 → 保存
   - 验证：自动跳转到新版本详情页，版本号显示 v2
   - 验证：版本演进区块显示"上一个版本：v1 · 标题"
   - 重新打开 v1 知识详情页 → 验证状态为「已废弃」，版本演进区块显示"已被新版本替换：查看新版本"
2. **空状态**：
   - 打开一个全新创建的 v1 知识（无上一版、无下一版）→ 版本演进区块不渲染
3. **错误状态**：
   - 在已 deprecated 的版本上 → 「创建新版本」按钮不显示
   - 标题或内容为空 → 保存按钮禁用 + toast 提示
4. **边界输入**：
   - 连续创建 v2、v3 → 验证版本链路逐级回溯正确
5. **重启客户端后数据恢复**：
   - 创建新版本后重启 → 验证 version 和 replaced_by_id 字段持久化

### 6.4 可维护性审核（AGENTS.md §4.5）
- [ ] KnowledgeVersionChain 和 ReplaceKnowledgeModal 职责单一
- [ ] KnowledgeDetailPage.tsx 行数未显著增加（控制在 510 行以内，理想是减少）
- [ ] 未在 UI 直接访问 Repository
- [ ] 类型明确（无 any、无 as any）
- [ ] 中文 UI 文案
- [ ] 使用 Heroicons 24px outline
- [ ] 错误处理统一（ServiceResult + toast）

---

## 七、风险与回滚

### 7.1 风险
1. **KnowledgeDetailPage.tsx 现有 488 行**：通过拆出 2 个子组件，行数应减少而非增加
2. **replaceKnowledge 非事务**：先插入新版本再 markReplacedBy，若中间失败会产生"新版本已插入但旧版本未标记"的中间态。可接受（用户可手动废弃旧版本），不引入事务封装以避免改动 db 层
3. **版本链路反查性能**：`findKnowledgeByReplacedById` 基于 `replaced_by_id` 字段查询，若后续知识量巨大需加索引。当前 MVP 阶段单项目知识量预期 < 1000，可暂不加索引

### 7.2 回滚方案
若实施失败需回滚：
1. 删除 2 个新建子组件文件
2. 还原 3 个改造文件（git checkout）
3. 数据库无 schema 变更，无需回滚迁移

---

## 八、实施顺序

1. **Step 1**：改造 `knowledgeRepository.ts`（新增 2 个方法）
2. **Step 2**：改造 `KnowledgeService.ts`（新增 replaceKnowledge + getPreviousVersion + 类型）
3. **Step 3**：新建 `KnowledgeVersionChain.tsx`
4. **Step 4**：新建 `ReplaceKnowledgeModal.tsx`
5. **Step 5**：改造 `KnowledgeDetailPage.tsx`（接入按钮 + 替换区块 + 渲染 Modal）
6. **Step 6**：运行 `pnpm typecheck` + `pnpm build` 验证
7. **Step 7**：返回最终总结报告
