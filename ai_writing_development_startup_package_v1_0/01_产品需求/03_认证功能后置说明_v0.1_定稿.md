# MVP 修订补丁：认证功能后置说明 v0.1

## 1. 结论

认证、登录、注册、账号系统、团队权限不作为 MVP 需求。

MVP 采用：

```text
单用户
本地优先
默认用户
默认工作空间
```

---

## 2. 默认身份

```text
DEFAULT_USER_ID=default_user
DEFAULT_WORKSPACE_ID=default_workspace
```

所有创建操作默认写入：

```text
created_by = default_user
updated_by = default_user
workspace_id = default_workspace
```

---

## 3. MVP 不提供的页面

```text
登录页
注册页
忘记密码
个人中心
团队管理
成员管理
权限设置
工作空间切换
```

客户端启动后直接进入：

```text
本地项目列表
```

---

## 4. API 调整

MVP 不需要：

```http
POST /api/v1/auth/login
POST /api/v1/auth/register
POST /api/v1/auth/logout
POST /api/v1/auth/refresh
GET /api/v1/me
PATCH /api/v1/me/preferences
GET /api/v1/workspaces
GET /api/v1/workspaces/{workspace_id}
```

模型配置路径简化为：

```http
GET /api/v1/model-providers
POST /api/v1/model-providers
PATCH /api/v1/model-providers/{provider_id}
DELETE /api/v1/model-providers/{provider_id}
POST /api/v1/model-providers/{provider_id}/test

GET /api/v1/model-configs
POST /api/v1/model-configs
PATCH /api/v1/model-configs/{model_config_id}
DELETE /api/v1/model-configs/{model_config_id}
```

---

## 5. 后续回归路线

| 阶段 | 能力 |
|---|---|
| P1 | 轻量登录、本地用户偏好 |
| P2 | 多工作空间 |
| P3 | 团队权限、企业 SSO、审计日志、私有部署 |
